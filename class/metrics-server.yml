parameters:
  kapitan:
    compile:
      - input_paths:
          - metrics-server/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - metrics-server/component/main.jsonnet
        input_type: jsonnet
        output_path: metrics-server/
